///
/// HybridNitroGeocoderSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `GeocoderResult` to properly resolve imports.
namespace margelo::nitro::nitrogeocoder { struct GeocoderResult; }

#include "GeocoderResult.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include <vector>

namespace margelo::nitro::nitrogeocoder {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NitroGeocoder`
   * Inherit this class to create instances of `HybridNitroGeocoderSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNitroGeocoder: public HybridNitroGeocoderSpec {
   * public:
   *   HybridNitroGeocoder(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNitroGeocoderSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNitroGeocoderSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNitroGeocoderSpec() override = default;

    public:
      // Properties
      virtual bool getIsGeocodingAvailable() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<GeocoderResult>> geocode(const std::string& address, const std::string& locale) = 0;
      virtual std::shared_ptr<Promise<GeocoderResult>> reverseGeocode(double latitude, double longitude, const std::string& locale) = 0;
      virtual std::shared_ptr<Promise<std::vector<GeocoderResult>>> geocodeMultiple(const std::string& address, double maxResults, const std::string& locale) = 0;
      virtual double calculateDistance(double lat1, double lon1, double lat2, double lon2) = 0;
      virtual std::shared_ptr<Promise<std::string>> reverseGeocodeSimple(double latitude, double longitude) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NitroGeocoder";
  };

} // namespace margelo::nitro::nitrogeocoder
