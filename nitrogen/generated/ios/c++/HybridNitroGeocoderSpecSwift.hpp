///
/// HybridNitroGeocoderSpecSwift.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#include "HybridNitroGeocoderSpec.hpp"

// Forward declaration of `HybridNitroGeocoderSpec_cxx` to properly resolve imports.
namespace NitroGeocoder { class HybridNitroGeocoderSpec_cxx; }

// Forward declaration of `GeocoderResult` to properly resolve imports.
namespace margelo::nitro::nitrogeocoder { struct GeocoderResult; }
// Forward declaration of `Position` to properly resolve imports.
namespace margelo::nitro::nitrogeocoder { struct Position; }
// Forward declaration of `Region` to properly resolve imports.
namespace margelo::nitro::nitrogeocoder { struct Region; }

#include "GeocoderResult.hpp"
#include <NitroModules/Promise.hpp>
#include "Position.hpp"
#include <string>
#include <NitroModules/Null.hpp>
#include "Region.hpp"
#include <variant>
#include <optional>
#include <vector>

#include "NitroGeocoder-Swift-Cxx-Umbrella.hpp"

namespace margelo::nitro::nitrogeocoder {

  /**
   * The C++ part of HybridNitroGeocoderSpec_cxx.swift.
   *
   * HybridNitroGeocoderSpecSwift (C++) accesses HybridNitroGeocoderSpec_cxx (Swift), and might
   * contain some additional bridging code for C++ <> Swift interop.
   *
   * Since this obviously introduces an overhead, I hope at some point in
   * the future, HybridNitroGeocoderSpec_cxx can directly inherit from the C++ class HybridNitroGeocoderSpec
   * to simplify the whole structure and memory management.
   */
  class HybridNitroGeocoderSpecSwift: public virtual HybridNitroGeocoderSpec {
  public:
    // Constructor from a Swift instance
    explicit HybridNitroGeocoderSpecSwift(const NitroGeocoder::HybridNitroGeocoderSpec_cxx& swiftPart):
      HybridObject(HybridNitroGeocoderSpec::TAG),
      _swiftPart(swiftPart) { }

  public:
    // Get the Swift part
    inline NitroGeocoder::HybridNitroGeocoderSpec_cxx& getSwiftPart() noexcept {
      return _swiftPart;
    }

  public:
    inline size_t getExternalMemorySize() noexcept override {
      return _swiftPart.getMemorySize();
    }
    void dispose() noexcept override {
      _swiftPart.dispose();
    }
    std::string toString() override {
      return _swiftPart.toString();
    }

  public:
    // Properties
    inline bool getIsGeocodingAvailable() noexcept override {
      return _swiftPart.isGeocodingAvailable();
    }

  public:
    // Methods
    inline std::shared_ptr<Promise<GeocoderResult>> geocode(const std::string& address, const std::string& locale) override {
      auto __result = _swiftPart.geocode(address, locale);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<GeocoderResult>> reverseGeocode(double latitude, double longitude, const std::string& locale) override {
      auto __result = _swiftPart.reverseGeocode(std::forward<decltype(latitude)>(latitude), std::forward<decltype(longitude)>(longitude), locale);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<std::vector<GeocoderResult>>> geocodeMultiple(const std::string& address, double maxResults, const std::string& locale) override {
      auto __result = _swiftPart.geocodeMultiple(address, std::forward<decltype(maxResults)>(maxResults), locale);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline double calculateDistance(double lat1, double lon1, double lat2, double lon2) override {
      auto __result = _swiftPart.calculateDistance(std::forward<decltype(lat1)>(lat1), std::forward<decltype(lon1)>(lon1), std::forward<decltype(lat2)>(lat2), std::forward<decltype(lon2)>(lon2));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<std::string>> reverseGeocodeSimple(double latitude, double longitude) override {
      auto __result = _swiftPart.reverseGeocodeSimple(std::forward<decltype(latitude)>(latitude), std::forward<decltype(longitude)>(longitude));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }

  private:
    NitroGeocoder::HybridNitroGeocoderSpec_cxx _swiftPart;
  };

} // namespace margelo::nitro::nitrogeocoder
